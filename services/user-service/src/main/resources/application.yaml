server:
  port: 5001
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/yt_airline_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

#for debugging only not for production
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

#For engine Health check using actuator
#management:
#  metrics:
#    enable:
#      hikari: true